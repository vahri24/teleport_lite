{{ define "dashboard_content" }}

<!-- Welcome Section -->
<div class="bg-white rounded-2xl shadow p-6 md:p-8 mb-6">
  <h2 class="text-2xl font-semibold mb-2">Dashboard</h2>
  <p class="text-slate-600">Welcome, <span class="font-medium">{{ .name }}</span> 🎉</p>
  <p class="text-slate-500 text-sm">Organization ID: {{ .org_id }}</p>
</div>

<!-- Quick Nav -->
<div class="bg-white rounded-2xl shadow p-6 mb-8">
  <h3 class="text-lg font-semibold mb-4">Quick Navigation</h3>
  <div class="flex justify-center gap-4 flex-wrap">
    <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-3">
      <li><a class="block rounded-xl border border-slate-200 px-4 py-3 hover:border-blue-400 hover:bg-blue-50 transition" href="/users">👤 Users</a></li>
      <li><a class="block rounded-xl border border-slate-200 px-4 py-3 hover:border-blue-400 hover:bg-blue-50 transition" href="/resources">📦 Resources</a></li>
      <li><a class="block rounded-xl border border-slate-200 px-4 py-3 hover:border-blue-400 hover:bg-blue-50 transition" href="/api/v1/audit" target="_blank">📜 Audit Trail</a></li>
    </ul>
  </div>
</div>

<!-- Live Data (with refresh buttons) -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
  <!-- Roles -->
  <div class="bg-white rounded-2xl shadow p-6">
    <div class="flex items-center justify-between mb-4">
      <h3 class="text-lg font-semibold">Roles</h3>
      <button id="refreshRoles" class="text-blue-600 text-sm hover:underline">↻ Refresh</button>
    </div>
    <div class="overflow-x-auto">
      <table class="min-w-full text-sm">
        <thead>
          <tr class="text-left text-slate-500 border-b">
            <th class="py-2 pr-4">ID</th>
            <th class="py-2 pr-4">Name</th>
            <th class="py-2 pr-4">Slug</th>
          </tr>
        </thead>
        <tbody id="rolesTable">
          <tr><td colspan="3" class="py-4 text-slate-400">Loading…</td></tr>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Resources -->
  <div class="bg-white rounded-2xl shadow p-6">
    <div class="mt-6 flex justify-center">
      <div class="flex flex-col items-center">
        <div class="relative w-40 h-40">
         <canvas id="resourcesChart"></canvas>
        </div>
        <p class="mt-2 text-slate-500 text-sm">Total Resources</p>
      </div>
    </div>
  </div>


  
</div>

{{ end }}
{{ template "layout" . }}
